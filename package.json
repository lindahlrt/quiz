{
  "name": "quiz-oignons",
  "version": "1.0.0",
  "scripts": {
    "sass:build": "sass scss/style.scss public/style.css --style=compressed --no-source-map",
    "sass:dev":   "sass scss/style.scss public/style.css --style=expanded",
    "sass:watch": "sass scss/style.scss public/style.css --watch --style=expanded"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
